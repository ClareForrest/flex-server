require 'rails_helper'

RSpec.describe "Services", type: :request do
    describe "GET services#index" do
      # before(:example) do
      #   @first_service = FactoryBot.create(:service)
      #   # @second_service = FactoryBot.create(:service)
      #   get addresses_index_path
      #   @json_response = JSON.parse(response.body)
      # end
  
      # it 'returns http success' do
      #   expect(response).to have_http_status(:success)
      # end
  
      # it 'contains the correct number of entries' do
      #   expect(@json_response['service'].count).to eq(2)
      # end
  
      # it 'address contains expected attributes' do
      #   expect(@json_response['services'].first).to include({
      #     'id' => @first_service.id, 
      #     'name' => @first_service.name,
      #     'cost' => @first_service.cost,
      #   })
      # end
    end
  end
  